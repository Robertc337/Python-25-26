import turtle
bob=turtle.Turtle()

def polygon (sides,distance):
    bob.color(color)
    angle = 360/sides

    for times in range (sides): 
        bob.forward(distance)
        bob.left(angle)
